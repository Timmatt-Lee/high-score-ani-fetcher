/**
 * Defines the specific step or field that failed during document parsing.
 */
export enum ScraperParseStep {
  PAGINATION,
  TITLE,
  WATCH_COUNT,
  EPISODE_COUNT,
  UPLOAD_DATE,
  SCORE,
  RATING_COUNT,
  DESCRIPTION,
}
